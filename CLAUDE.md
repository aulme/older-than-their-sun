The canonical spec is `DESIGN_NOTES.md`; read it before changing the simulation.
Proposals live in `specs/proposals/` (absorbed ones in `specs/proposals/done/`); the plan that built Simulation v2, with its ledger and optimisation notes, is `specs/done/plan.md`.
Commits end with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
